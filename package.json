{
  "name": "plan-and-plate-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix services/barcode && npm install --prefix services/groceryListManager && npm install --prefix services/mealPlanner && npm install --prefix services/userManagement",
    "dev": "npx concurrently --prefix-colors \"auto\" --names \"CLIENT,BARCODE,GROCERY,MEAL,USER\" \"npm run dev --prefix client\" \"npm run dev --prefix services/barcode\" \"npm run dev --prefix services/groceryListManager\" \"npm run dev --prefix services/mealPlanner\" \"npm run dev --prefix services/userManagement\"",
    "start-all": "npm run dev",
    "seed-mock": "npm run seed --prefix services/mealPlanner"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
